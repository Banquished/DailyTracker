version: '3.9'

services:
  api:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
    # In production, serve built files through nginx or a CDN
    # Remove dev volume mounts
    volumes: []
